# dev-notes — 踩坑记录（顺手就写）

> 目的：**专门放「会话里实际踩到的坑」**，按问题/主题一条一记，遇到就写，
> 不必等你问。用途：给未来的 AI / 自己快速定位**为什么这里会这样**。
>
> 与其它文档的关系：
> - `docs/dev-playbook.md` = **防重复踩坑的浓缩速查**（更短、更稳、先读它）；
> - `docs/iteration-plan.md` = **迭代轮次的事实记录/验证结论**；
> - `docs/android-build.md` 等 = 单主题**唯一真源**；
> - **本目录 `dev-notes/`** = 上面的「原料库」：**具体问题 + 排查过程 + 最终根因**。
>   每一步踩坑都在这里留一手，然后**把它归纳进 dev-playbook**（避免只在这里沉底）。
>
> **命名**：`YYYY-MM-DD-<主题连写>.md`。**索引**：在 `README.md`（本文件）追加一行。

## 坑索引

| 日期 | 主题 | 根因一句话 | 状态 |
|------|------|-----------|------|
| 2026-08-31 | 屏幕共享帧率上不去（~5fps） | 瓶颈在 serve 侧 Rust 双线性缩放 → 改成 ffmpeg swscale 缩放，serve 3.5%CPU/源~23fps | 已修复 |
| 2026-08-31 | 手机端播放显示（~11fps）改造 | base64 事件 → 二进制 Channel + 原始 RGBA；12 位定点双线性转换，Rust JNI 零拷贝直传 | 已优化 |
| 2026-08-31 | 手机全屏与横竖屏自适应 | 画面宽高比自适应驱动 Android `requestedOrientation`（`SENSOR_LANDSCAPE`/`SENSOR_PORTRAIT`） | 已修复 |
| 2026-09-01 | 手机端 MediaCodec 启动与播放规范 | 消除初始化竞态、安全配置回退、常亮与音频焦点 Ducking 智能避让 | 已落地 |
| 2026-08-31 | 完美退出 | 停止 serve/流时 ffmpeg 子进程报 Broken pipe、清理不干净（需要优雅收尾） | 排期 |
| 2026-08-31 | 测试时系统卡顿 | 压测/联调时主机（serve CPU 高）导致系统卡顿，优化 | 排期 |
| 2026-08-31 | 排查性能瓶颈 | 传输/渲染/编码全链路性能瓶颈继续排查 | 排期 |
